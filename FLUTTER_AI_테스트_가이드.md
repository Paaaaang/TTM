# Flutter 앱 AI 분석 테스트 가이드

## 🚀 빠른 시작 가이드

### 1단계: 백엔드 서버 실행

**새 PowerShell 창 열기** (Ctrl + Shift + P → "PowerShell: 새 통합 터미널")

```powershell
cd C:\Users\smhrd\Desktop\App\ttm\backend
C:\Users\smhrd\Desktop\App\ttm\.venv\Scripts\python.exe main.py
```

**확인할 메시지**:
```
✅ MySQL 데이터베이스 연결 성공
INFO:     Uvicorn running on http://0.0.0.0:3000
INFO:     Application startup complete.
```

### 2단계: Flutter 앱 실행

**다른 터미널 창에서**:
```powershell
cd C:\Users\smhrd\Desktop\App\ttm
flutter run
```

### 3단계: 앱에서 AI 분석 테스트

#### 📸 음식 사진 촬영
1. 앱 하단 네비게이션에서 **"식단"** 탭 선택
2. **"+ 추가"** 버튼 클릭
3. 식사 유형 선택 (아침/점심/저녁/간식)
4. **"카메라로 음식 촬영"** 선택
5. 음식 사진 촬영 (또는 갤러리에서 선택)

#### 🤖 AI 분석 과정
사진 촬영 후 자동으로:
```
1. "AI가 음식을 분석하고 있습니다..." 로딩 화면 표시
2. 백엔드로 이미지 전송
3. AI 분석 수행:
   - YOLO 탐지 (또는 Mock: "밥")
   - ResNet 양 추정
   - 영양 DB 조회
4. 결과 화면 표시
```

#### ✅ 예상 결과
```
음식 이름: 쌀밥 (또는 다른 음식)
칼로리: 167.4 kcal
탄수화물: 36.86 g
단백질: 2.88 g
지방: 0.22 g
당류: 0.0 g
나트륨: 29.7 mg
```

## 🔍 문제 해결

### "분석 실패" 에러가 뜨는 경우

#### 1. 백엔드 서버 확인
```powershell
# 브라우저에서 접속 테스트
http://localhost:3000
```

정상이면: `{"detail":"Not Found"}` 메시지 표시

#### 2. 네트워크 설정 확인
**파일**: `lib/constants/api_config.dart`

**에뮬레이터/실제 기기에 따라 다름**:
- Android 에뮬레이터: `10.0.2.2:3000`
- iOS 시뮬레이터: `localhost:3000`
- 실제 기기: PC의 IP 주소 (예: `192.168.0.10:3000`)

**현재 IP 확인**:
```powershell
ipconfig | Select-String "IPv4"
```

#### 3. 백엔드 로그 확인
백엔드 터미널에서 에러 확인:
```
🤖 AI 식사 이미지 분석 요청
✅ 이미지 저장: ...
🍽️ 식사 이미지 분석 시작
...
```

### "음식을 인식할 수 없습니다" 메시지

**정상 동작입니다!**

현재 YOLO가 Mock 모드로 작동 중이므로:
- 모든 음식을 "밥"으로 인식
- ResNet으로 양만 추정
- 영양 DB에서 "밥" 관련 정보 반환

## 📊 테스트 체크리스트

### 필수 테스트
- [ ] 백엔드 서버 정상 실행
- [ ] Flutter 앱 정상 실행
- [ ] 카메라 촬영 가능
- [ ] AI 분석 로딩 화면 표시
- [ ] 분석 결과 화면 표시
- [ ] 영양 정보 수치 표시
- [ ] "수정" 버튼으로 값 변경 가능
- [ ] "식단에 추가하기" 버튼 작동

### 추가 테스트
- [ ] 다양한 음식 사진 테스트
- [ ] 여러 번 촬영 반복 테스트
- [ ] 오류 발생 시 재촬영 가능
- [ ] 결과 저장 후 식단 목록에 표시

## 🎬 전체 동작 흐름

```
┌─────────────────┐
│  사용자 액션    │
│  (사진 촬영)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Flutter 앱     │
│  MealService    │
│  .analyzeMeal   │
│  Image()        │
└────────┬────────┘
         │ POST /api/meals/analyze-image
         │ (이미지 + member_id + meal_type)
         ▼
┌─────────────────┐
│  FastAPI        │
│  백엔드         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  FoodAnalyzer   │
│  1. YOLO/Mock   │
│  2. ResNet      │
│  3. 영양 DB     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  JSON 응답      │
│  {success,      │
│   foods: [...]} │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  AI 분석 결과   │
│  화면 표시      │
│  (수정 가능)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  식단에 추가    │
│  DB 저장        │
└─────────────────┘
```

## 📝 주의사항

### 현재 제한사항
1. **YOLO 비활성화**: 모든 음식을 "밥"으로 인식
2. **단일 음식 탐지**: 한 번에 1개 음식만 분석
3. **Mock 데이터**: 실제 음식 탐지 불가

### 향후 개선 필요
1. YOLO 모듈 임포트 오류 수정
2. 다중 음식 탐지 지원
3. 더 다양한 음식 데이터베이스
4. 탐지 정확도 향상

## ✨ 성공 기준

테스트가 성공했다고 판단할 수 있는 조건:

✅ 사진 촬영 → 로딩 → 결과 표시까지 에러 없이 완료
✅ 영양 정보가 숫자로 정상 표시됨
✅ 수정 기능이 작동함
✅ 식단에 추가 시 목록에 표시됨

## 🆘 추가 도움이 필요한 경우

### 로그 확인 위치
1. **백엔드 로그**: PowerShell 터미널
2. **Flutter 로그**: `flutter run` 실행한 터미널
3. **브라우저 네트워크**: Chrome DevTools (웹 버전)

### 테스트 스크립트
```powershell
# 백엔드 API 직접 테스트
cd C:\Users\smhrd\Desktop\App\ttm\backend
C:\Users\smhrd\Desktop\App\ttm\.venv\Scripts\python.exe test_api_endpoint.py
```

---

## 🎯 지금 바로 시작하세요!

1. **터미널 1**: 백엔드 실행
2. **터미널 2**: Flutter 앱 실행
3. **앱에서**: 사진 촬영 → AI 분석 확인!
