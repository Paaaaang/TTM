# TTM 프로젝트 경로 점검 및 수정 보고서

**작성일**: 2026-01-15  
**대상**: 전체 프로젝트 폴더 구조 및 경로 참조

---

## 📋 점검 개요

프로젝트 폴더 이동 시 발생할 수 있는 경로 관련 문제를 사전에 점검하고 수정하였습니다.

---

## ✅ 점검 및 수정 항목

### 1. **백엔드 경로 문제 점검 및 수정**

#### 📁 main.py (루트 서버 파일)
- ✅ **상태**: 정상
- **경로 설정**:
  ```python
  BASE_DIR = Path(__file__).resolve().parent
  UPLOAD_DIR = BASE_DIR / "uploads"
  app.mount("/uploads", StaticFiles(directory=str(UPLOAD_DIR)), name="uploads")
  ```
- **설명**: 절대 경로 기반으로 설정되어 있어 폴더 이동 시에도 안전

---

#### 📁 routers/members.py
- ✅ **수정 완료**
- **변경 사항**:
  ```python
  # 수정 전 (상대 경로)
  upload_dir = Path("uploads/profiles")
  
  # 수정 후 (절대 경로)
  BASE_DIR = Path(__file__).resolve().parent.parent
  UPLOAD_DIR = BASE_DIR / "uploads"
  upload_dir = UPLOAD_DIR / "profiles"
  ```
- **영향**: 프로필 이미지 업로드 경로 안정성 향상

---

#### 📁 routers/posts.py
- ✅ **수정 완료**
- **변경 사항**:
  ```python
  # 수정 전 (상대 경로)
  upload_dir = Path("uploads/posts")
  
  # 수정 후 (절대 경로)
  BASE_DIR = Path(__file__).resolve().parent.parent
  UPLOAD_DIR = BASE_DIR / "uploads"
  upload_dir = UPLOAD_DIR / "posts"
  ```
- **영향**: 게시글 이미지 업로드 경로 안정성 향상

---

#### 📁 routers/meals.py
- ✅ **수정 완료**
- **변경 사항**:
  ```python
  # 수정 전 (상대 경로)
  upload_dir = Path("uploads/meals")
  
  # 수정 후 (절대 경로)
  BASE_DIR = Path(__file__).resolve().parent.parent
  UPLOAD_DIR = BASE_DIR / "uploads"
  upload_dir = UPLOAD_DIR / "meals"
  ```
- **영향**: 식사 이미지 업로드 및 AI 분석 경로 안정성 향상

---

#### 📁 services/food_analyzer.py
- ✅ **상태**: 정상
- **경로 설정**:
  ```python
  BASE_DIR = os.path.dirname(os.path.dirname(__file__))
  NUTRITION_DB_PATH = os.path.join(BASE_DIR, "data", "nutrition_db.xlsx")
  YOLO_MODEL_PATH = os.path.join(BASE_DIR, "ai_models", "yolo", "best_food_model.pt")
  RESNET_MODEL_PATH = os.path.join(BASE_DIR, "ai_models", "resnet", "quantity_model.pth")
  ```
- **설명**: 절대 경로 기반으로 AI 모델 및 데이터 파일 참조

---

#### 📁 config/database.py
- ✅ **상태**: 정상
- **환경 변수 사용**:
  ```python
  load_dotenv()  # .env 파일에서 환경 변수 로드
  DB_HOST = os.getenv("DB_HOST")
  DB_USER = os.getenv("DB_USER")
  DB_PASSWORD = os.getenv("DB_PASSWORD")
  DB_NAME = os.getenv("DB_NAME")
  DB_PORT = int(os.getenv("DB_PORT", 3306))
  ```
- **설명**: 하드코딩된 경로 없이 환경 변수로 관리

---

### 2. **프론트엔드 경로 점검**

#### 📱 lib/constants/api_config.dart
- ✅ **상태**: 정상
- **API 기본 URL**:
  ```dart
  static const String baseUrl = 'http://localhost:3000';
  ```
- **설명**: 환경에 따라 수정 가능한 상수로 정의
- **프로덕션 배포 시**: baseUrl을 실제 서버 URL로 변경 필요

---

#### 📱 lib/ 내부 import 경로
- ✅ **상태**: 정상
- **패키지 기반 import 사용**:
  ```dart
  import 'package:ttm/services/auth_service.dart';
  import 'package:ttm/models/user.dart';
  import 'package:ttm/constants/api_config.dart';
  ```
- **설명**: Flutter의 package: 방식으로 절대 경로 사용, 폴더 이동 시에도 안전

---

#### 🎨 assets 경로 (pubspec.yaml)
- ✅ **상태**: 정상
- **설정**:
  ```yaml
  assets:
    - assets/images/
    - assets/icons/
  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Regular.ttf
  ```
- **실제 파일 존재 여부**: ✅ 확인됨
  - `assets/icons/logoIcon.png` ✅ 존재
  - `assets/fonts/Pretendard-*.ttf` ✅ 존재

---

### 3. **환경 변수 및 설정 파일**

#### 🔐 backend/.env
- ✅ **상태**: 정상
- **주요 설정**:
  ```env
  PORT=3000
  DB_HOST=project-db-campus.smhrd.com
  DB_PORT=3307
  DB_NAME=we0123
  JWT_SECRET=ttm_secret_key_2026_change_in_production
  GOOGLE_API_KEY=AIzaSyC***
  ```
- **주의사항**: 
  - ⚠️ 이 파일은 `.gitignore`에 포함되어 있음
  - 새 환경에 배포 시 `.env` 파일을 별도로 생성해야 함

---

### 4. **업로드 디렉토리 구조**

#### 📂 backend/uploads/
```
uploads/
├── meals/          # 식사 이미지 (AI 분석용)
├── posts/          # 게시글 이미지
├── profiles/       # 프로필 이미지
└── .gitkeep        # Git에서 빈 폴더 유지
```

- ✅ **자동 생성**: 각 라우터에서 `upload_dir.mkdir(parents=True, exist_ok=True)` 사용
- ✅ **보안**: `.gitignore`에 포함되어 실제 업로드 파일은 Git에 포함 안 됨

---

## 🔍 잠재적 문제 지점 (주의 필요)

### 1. **아카이브 파일들 (utilities/archives/)**
- 📌 **위치**: `backend/utilities/archives/converters/`
- **상태**: 사용되지 않는 아카이브 파일들
- **경로 패턴**:
  ```python
  excel_path = os.path.join(os.path.dirname(os.path.dirname(__file__)), "data", "nutrition_db.xlsx")
  ```
- **권장사항**: 아카이브 폴더는 실제 운영에 사용되지 않으므로 문제 없음

---

### 2. **마이그레이션 스크립트 (utilities/migrations/)**
- 📌 **위치**: `backend/utilities/migrations/add_missing_columns.py`
- **경로 패턴**:
  ```python
  sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
  ```
- **권장사항**: 마이그레이션은 일회성이므로 실행 시 경로 확인 후 사용

---

## 📊 수정 통계

| 구분 | 점검 항목 | 수정 필요 | 수정 완료 |
|------|-----------|-----------|-----------|
| **백엔드** | 라우터 파일 (11개) | 3개 | ✅ 3개 |
| **백엔드** | 서비스 파일 (5개) | 0개 | - |
| **백엔드** | 설정 파일 (2개) | 0개 | - |
| **프론트엔드** | Dart 파일 (50+개) | 0개 | - |
| **프론트엔드** | Assets | 0개 | - |
| **환경 설정** | .env, pubspec.yaml | 0개 | - |
| **합계** | **70+개** | **3개** | **✅ 3개** |

---

## 🎯 결론

### ✅ 안전성 확보
1. **백엔드 업로드 경로**: 모두 절대 경로로 변경 완료
2. **프론트엔드 import**: package 기반 절대 경로 사용 중
3. **Assets**: pubspec.yaml에 올바르게 설정됨
4. **환경 변수**: .env 파일로 분리되어 있어 이식성 우수

### 🚀 폴더 이동 시 필요한 작업
프로젝트를 새 경로로 이동할 경우:

1. **백엔드**:
   ```bash
   cd backend
   # .env 파일 확인 (없으면 생성)
   python main.py  # 서버 시작 시 uploads/ 폴더 자동 생성됨
   ```

2. **프론트엔드**:
   ```bash
   flutter pub get  # 의존성 재설치
   flutter run      # 앱 실행
   ```

3. **데이터베이스**: `.env` 파일의 DB 설정만 확인하면 됨

---

## ⚠️ 주의사항

### 프로덕션 배포 시
1. **백엔드**:
   - `.env` 파일을 별도로 관리하고 복사
   - `uploads/` 폴더 권한 설정 (쓰기 권한 필요)

2. **프론트엔드**:
   - `lib/constants/api_config.dart`의 `baseUrl`을 실제 서버 URL로 변경
   - Firebase 설정 파일 확인 (google-services.json 등)

3. **보안**:
   - `.env` 파일이 Git에 포함되지 않도록 주의
   - JWT_SECRET, GOOGLE_API_KEY 등 민감 정보 관리

---

**✅ 경로 점검 완료**  
모든 주요 경로가 절대 경로 기반으로 설정되어 있어, 프로젝트 폴더 이동 시에도 안전하게 작동합니다.
